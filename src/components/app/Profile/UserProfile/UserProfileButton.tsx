import { StyleSheet, Text, View } from "react-native";
import React from "react";

const UserProfileButton = () => {
  return (
    <View>
      <Text>UserProfileButton</Text>
    </View>
  );
};

export default UserProfileButton;

const styles = StyleSheet.create({});
